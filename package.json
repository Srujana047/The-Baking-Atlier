{
  "name": "the-baking-atlier",
  "private": true,
  "version": "0.1.0",
  "description": "Phase 1: Full-stack bakery community app (React + Express + MongoDB).",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c blue,magenta \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "start": "npm run start --workspace server",
    "build": "npm run build --workspace client",
    "lint": "npm run lint --workspace client && npm run lint --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "dependencies": {
    "mongoose": "^9.6.2",
    "react-icons": "^5.6.0"
  }
}
